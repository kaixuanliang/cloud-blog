spring:
  application:
    name: blog-producer-manage
  profiles: # 环境
    active: dev
  cloud:
    nacos:
      server-addr: localhost:8848 #nacos地址
      config:
        file-extension: yaml #文件后缀名
